# Dependencias de Microsoft OmniParser V2 (detección visual del botón
# "reproducir audio" en WhatsApp Web -- ver ia_local/omniparser_detector.py).
#
# Separadas de requirements.txt a propósito: son pesadas (torch, ultralytics,
# easyocr, paddleocr, transformers) y solo las necesitas si usas esta función
# específica de detección de audio por visión, no para el resto del pipeline.
#
# OmniParser V2 en sí NO se instala desde aquí -- hay que clonar el repo y
# descargar sus pesos por separado. Ver el docstring completo en
# ia_local/omniparser_detector.py para los pasos exactos.
#
# Instalación:
#   pip install -r requirements-omniparser.txt

torch>=2.1
torchvision>=0.16
ultralytics==8.3.70

# Florence-2 es incompatible con transformers>=4.44
# (AttributeError en Florence2LanguageConfig.__init__).
transformers>=4.36,<4.44

easyocr>=1.7

# paddleocr 3.x quitó parámetros que util/utils.py de OmniParser aún usa.
paddleocr>=2.7,<3.0
paddlepaddle>=2.5,<3.0

supervision==0.18.0
opencv-python>=4.8
huggingface_hub>=0.20
einops>=0.8
timm>=1.0
accelerate>=0.30

# util/utils.py importa openai a nivel de módulo aunque no lo usemos.
openai>=1.3
matplotlib>=3.7
